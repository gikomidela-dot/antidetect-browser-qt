# 🤖 Автоматическая сборка через GitHub Actions

## Как получить Windows сборку БЕЗ Windows:

### Шаг 1: Загрузите проект на GitHub

```bash
cd /home/x/.qodo/antidetect-browser-qt

# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Сделайте коммит
git commit -m "Initial commit with Windows build support"

# Создайте репозиторий на GitHub и добавьте remote
git remote add origin https://github.com/YOUR_USERNAME/antidetect-browser-qt.git

# Отправьте код
git push -u origin main
```

### Шаг 2: GitHub Actions автоматически соберет проект

После push на GitHub:
1. Перейдите в **Actions** tab вашего репозитория
2. Увидите запущенный workflow "Build Windows Release"
3. Подождите ~15-20 минут пока соберется
4. Скачайте готовый **AntidetectBrowser-Windows-x64.zip** из Artifacts

### Шаг 3: Скачайте готовую сборку

1. Откройте: `https://github.com/YOUR_USERNAME/antidetect-browser-qt/actions`
2. Кликните на последний успешный build (зеленая галочка ✅)
3. Прокрутите вниз до секции **Artifacts**
4. Скачайте **AntidetectBrowser-Windows-x64.zip**

---

## 🎯 Ручной запуск сборки

Если хотите собрать вручную:

1. Перейдите в **Actions** → **Build Windows Release**
2. Нажмите **Run workflow** → **Run workflow**
3. Подождите завершения
4. Скачайте artifact

---

## 📦 Что вы получите:

```
AntidetectBrowser-Windows-x64.zip (~70 MB)
└── AntidetectBrowser-Windows-x64/
    ├── AntidetectBrowser.exe
    ├── Qt6Core.dll
    ├── Qt6Gui.dll
    ├── Qt6WebEngineCore.dll
    ├── libssl-3-x64.dll
    ├── libcrypto-3-x64.dll
    ├── platforms/
    ├── translations/
    └── README.txt
```

**Полностью готово к использованию!** Просто распакуйте и запустите.

---

## 🔄 Автоматические релизы

Чтобы создать релиз:

```bash
# Создайте тег
git tag v1.0.0

# Отправьте тег
git push origin v1.0.0
```

GitHub Actions автоматически:
1. Соберет проект
2. Создаст Release
3. Прикрепит ZIP файл к релизу

Релиз будет доступен по адресу:
`https://github.com/YOUR_USERNAME/antidetect-browser-qt/releases`

---

## ⚙️ Настройка workflow

Файл находится в: `.github/workflows/build-windows.yml`

Можно изменить:
- Версию Qt (по умолчанию 6.5.3)
- Количество ядер для сборки (-j4)
- Дополнительные флаги CMake

---

## 🐛 Troubleshooting

### ❌ Build failed
**Причина:** Ошибка компиляции
**Решение:** Проверьте логи в Actions, исправьте код, сделайте новый commit

### ❌ Artifact not found
**Причина:** Build не завершился успешно
**Решение:** Проверьте что все шаги прошли с зеленой галочкой

### ❌ Qt not found
**Причина:** Проблема с install-qt-action
**Решение:** Обновите версию action в workflow файле

---

## 💰 Стоимость

GitHub Actions **БЕСПЛАТНО** для публичных репозиториев:
- ✅ Unlimited minutes
- ✅ Unlimited builds
- ✅ Unlimited artifacts (90 дней хранения)

Для приватных репозиториев:
- 2000 минут/месяц бесплатно
- ~15-20 минут на одну сборку
- = ~100 сборок в месяц бесплатно

---

## 🚀 Альтернативы GitHub Actions

### 1. AppVeyor
```yaml
# appveyor.yml
image: Visual Studio 2022
platform: x64
configuration: Release

install:
  - choco install qt6 cmake openssl

build_script:
  - mkdir build && cd build
  - cmake .. -G "MinGW Makefiles"
  - cmake --build .

artifacts:
  - path: build\*.exe
```

### 2. CircleCI
```yaml
# .circleci/config.yml
version: 2.1
jobs:
  build-windows:
    executor:
      name: win/default
    steps:
      - checkout
      - run: choco install qt6 cmake
      - run: cmake --build .
```

### 3. Azure Pipelines
Бесплатно для open source проектов

---

## 📝 Checklist

- [ ] Код загружен на GitHub
- [ ] Файл `.github/workflows/build-windows.yml` существу��т
- [ ] Actions включены в настройках репозитория
- [ ] Build запущен и завершился успешно
- [ ] Artifact скачан и протестирован

---

**Готово!** Теперь у вас есть автоматическая Windows сборка без необходимости иметь Windows! 🎉
